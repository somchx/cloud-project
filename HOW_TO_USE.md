# 🎯 วิธีใช้งานโปรเจกต์ POS ร้านขายของชำ

## 📋 สารบัญ

1. [วิธีรันครั้งแรก](#วิธีรันครั้งแรก)
2. [วิธีใช้งานระบบ](#วิธีใช้งานระบบ)
3. [วิธี Deploy](#วิธี-deploy)
4. [วิธี Push ไป GitHub](#วิธี-push-ไป-github)
5. [เอกสารเพิ่มเติม](#เอกสารเพิ่มเติม)

---

## 🚀 วิธีรันครั้งแรก

### ขั้นตอนที่ 1: รันระบบ

**สำหรับ macOS/Linux:**
```bash
cd /Users/somchx/Desktop/pos-cloud
./start.sh
```

**สำหรับ Windows:**
```bash
cd \Users\somchx\Desktop\pos-cloud
start.bat
```

### ขั้นตอนที่ 2: เปิดใช้งาน

รอประมาณ 30-60 วินาที จากนั้นเปิด Browser:

- **เว็บไซต์หลัก**: http://localhost:3000
- **API Backend**: http://localhost:8000
- **เอกสาร API**: http://localhost:8000/docs

### ขั้นตอนที่ 3: ทดสอบระบบ

1. เปิด http://localhost:3000
2. คลิกที่ "จัดการสินค้า" ในเมนู
3. ลองเพิ่มสินค้าใหม่
4. ไปที่ "ขายของ (POS)" และลองขายสินค้า
5. กลับมาดู Dashboard

---

## 💡 วิธีใช้งานระบบ

### 1. หน้า Dashboard (หน้าหลัก)

**ดูได้ที่**: http://localhost:3000/

**ฟีเจอร์:**
- ยอดขายวันนี้ (บาท)
- จำนวนบิลวันนี้
- สินค้าขายดี Top 5

**ข้อมูลจะอัพเดททันทีหลังมีการขาย**

---

### 2. หน้าขายของ (POS)

**ดูได้ที่**: http://localhost:3000/pos

**วิธีใช้:**

#### ก. เลือกสินค้า
- ดูสินค้าทั้งหมดในตาราง
- ใช้ช่องค้นหาเพื่อหาสินค้า
- กรองตามหมวดหมู่
- **คลิกที่การ์ดสินค้า** เพื่อเพิ่มลงตะกร้า

#### ข. จัดการตะกร้า
- ปุ่ม **+** เพิ่มจำนวน
- ปุ่ม **-** ลดจำนวน
- ระบบจะคำนวณยอดรวมอัตโนมัติ

#### ค. ชำระเงิน
1. คลิกปุ่ม **"💳 ชำระเงิน"**
2. ตรวจสอบยอดรวม
3. คลิก **"ยืนยัน"**
4. ระบบจะบันทึกการขายและลดสต็อกสินค้าอัตโนมัติ

#### ด. ล้างตะกร้า
- คลิกปุ่ม **"🗑️ ล้างตะกร้า"** เพื่อเริ่มต้นใหม่

---

### 3. หน้าจัดการสินค้า

**ดูได้ที่**: http://localhost:3000/products

**วิธีเพิ่มสินค้าใหม่:**

1. กรอกข้อมูลในฟอร์ม:
   - **ชื่อสินค้า**: เช่น "น้ำดื่ม 600ml"
   - **ราคา**: เช่น 7.50
   - **หมวดหมู่**: เลือกจาก dropdown
   - **จำนวนคงเหลือ**: เช่น 100
   - **URL รูปภาพ**: (ไม่บังคับ)

2. คลิกปุ่ม **"➕ เพิ่มสินค้า"**

**วิธีแก้ไขสินค้า:**

1. หาสินค้าที่ต้องการในตาราง
2. คลิกปุ่ม **"✏️ แก้ไข"**
3. แก้ไขข้อมูลในฟอร์มด้านบน
4. คลิกปุ่ม **"💾 บันทึกการแก้ไข"**

**วิธีลบสินค้า:**

1. หาสินค้าที่ต้องการในตาราง
2. คลิกปุ่ม **"🗑️ ลบ"**
3. ยืนยันการลบ

---

## ☁️ วิธี Deploy

### Backend → AWS Elastic Beanstalk

```bash
# 1. เข้าโฟลเดอร์ backend
cd backend

# 2. ติดตั้ง EB CLI (ครั้งเดียว)
pip install awsebcli

# 3. Deploy
eb init -p python-3.11 pos-backend --region us-east-1
eb create pos-backend-env

# 4. URL ของ Backend จะแสดงหลัง deploy สำเร็จ
# ตัวอย่าง: http://pos-backend-env.eba-xxxxx.us-east-1.elasticbeanstalk.com
```

**📖 รายละเอียด**: อ่านไฟล์ `DEPLOY_BACKEND.md`

---

### Frontend → AWS S3

```bash
# 1. เข้าโฟลเดอร์ frontend
cd frontend

# 2. ตั้งค่า API URL (แทน your-backend-url ด้วย URL จริง)
echo "REACT_APP_API_URL=http://your-backend-url.elasticbeanstalk.com" > .env.production

# 3. Build
npm run build

# 4. สร้าง S3 bucket
aws s3 mb s3://pos-grocery-frontend

# 5. Upload
aws s3 sync build/ s3://pos-grocery-frontend --acl public-read

# 6. เปิด Static Website Hosting
aws s3 website s3://pos-grocery-frontend --index-document index.html
```

**URL Frontend**: `http://pos-grocery-frontend.s3-website-us-east-1.amazonaws.com`

**📖 รายละเอียด**: อ่านไฟล์ `DEPLOY_FRONTEND.md`

---

## 🐙 วิธี Push ไป GitHub

### ครั้งแรก (Initial Push)

```bash
# 1. เข้าโฟลเดอร์โปรเจกต์
cd /Users/somchx/Desktop/pos-cloud

# 2. Initialize Git
git init

# 3. เชื่อม GitHub
git remote add origin https://github.com/somchx/cloud-project.git

# 4. เพิ่มไฟล์ทั้งหมด
git add .

# 5. Commit
git commit -m "Initial POS grocery system"

# 6. Push
git branch -M main
git push -u origin main
```

### ครั้งถัดไป (Update)

```bash
# หลังแก้ไขโค้ด...

# 1. เพิ่มไฟล์ที่เปลี่ยนแปลง
git add .

# 2. Commit พร้อมข้อความอธิบาย
git commit -m "Add new feature: payment system"

# 3. Push
git push origin main
```

**📖 รายละเอียด**: อ่านไฟล์ `GIT_GUIDE.md`

---

## 📚 เอกสารเพิ่มเติม

| ไฟล์ | สำหรับอะไร |
|------|------------|
| 📘 `PROJECT_SUMMARY.md` | **เริ่มต้นอ่านที่นี่** - สรุปทุกอย่าง |
| 🚀 `QUICKSTART.md` | วิธีรันแบบด่วน |
| 💻 `INSTALLATION.md` | วิธีติดตั้งแบบละเอียด |
| 📖 `README.md` | คู่มือหลักของโปรเจกต์ |
| 🔌 `API_DOCUMENTATION.md` | เอกสาร API ครบถ้วน |
| ☁️ `DEPLOY_BACKEND.md` | วิธี Deploy Backend |
| ☁️ `DEPLOY_FRONTEND.md` | วิธี Deploy Frontend |
| 🐙 `GIT_GUIDE.md` | คู่มือใช้ Git อย่างละเอียด |
| 📊 `SAMPLE_DATA.md` | ข้อมูลตัวอย่างที่มีในระบบ |

---

## 🛠️ คำสั่งที่ใช้บ่อย

### Backend

```bash
cd backend

# รัน Backend
source venv/bin/activate  # macOS/Linux
venv\Scripts\activate     # Windows
uvicorn main:app --reload

# สร้างข้อมูลตัวอย่างใหม่
python seed_data.py

# ติดตั้ง dependencies ใหม่
pip install -r requirements.txt
```

### Frontend

```bash
cd frontend

# รัน Frontend
npm start

# Build production
npm run build

# ติดตั้ง dependencies ใหม่
npm install
```

### Git

```bash
# ดูสถานะ
git status

# ดูประวัติ
git log --oneline

# Pull อัพเดทล่าสุด
git pull origin main

# Push
git add .
git commit -m "Your message"
git push origin main
```

---

## ❓ FAQ

### Q: จะหยุดระบบยังไง?
**A:** กด `Ctrl + C` ใน Terminal ที่รัน Backend และ Frontend

### Q: ข้อมูลหายหมด?
**A:** รันคำสั่ง:
```bash
cd backend
python seed_data.py
```

### Q: Frontend เชื่อมต่อ Backend ไม่ได้?
**A:** ตรวจสอบว่า:
1. Backend รันอยู่ที่ http://localhost:8000
2. ไฟล์ `frontend/.env` มี `REACT_APP_API_URL=http://localhost:8000`
3. ลอง refresh หน้าเว็บ

### Q: Port 8000 หรือ 3000 ถูกใช้แล้ว?
**A:** 
```bash
# macOS/Linux
lsof -i :8000
kill -9 <PID>

# Windows
netstat -ano | findstr :8000
taskkill /PID <PID> /F
```

### Q: จะเพิ่มข้อมูลตัวอย่างเพิ่ม?
**A:** แก้ไขไฟล์ `backend/seed_data.py` แล้วรันใหม่

---

## 📞 ติดต่อ/รายงานปัญหา

- **GitHub Repository**: https://github.com/somchx/cloud-project
- **Report Issues**: https://github.com/somchx/cloud-project/issues

---

## ✅ Checklist การใช้งาน

### ก่อนเริ่มใช้งาน
- [ ] รัน `./start.sh` หรือ `start.bat` สำเร็จ
- [ ] เปิด http://localhost:3000 ได้
- [ ] เปิด http://localhost:8000/docs ได้
- [ ] ระบบมีข้อมูลตัวอย่าง

### ก่อน Deploy
- [ ] ทดสอบระบบบน Local แล้ว
- [ ] ตั้งค่า AWS CLI แล้ว
- [ ] สร้าง S3 bucket แล้ว (สำหรับ Frontend)
- [ ] อัพเดท API URL ใน `.env.production`

### ก่อน Push GitHub
- [ ] ตรวจสอบว่าไม่มีไฟล์ sensitive (รหัสผ่าน, API keys)
- [ ] ทดสอบว่าระบบรันได้
- [ ] เขียน commit message ที่ชัดเจน

---

## 🎉 เริ่มต้นใช้งานเลย!

```bash
cd /Users/somchx/Desktop/pos-cloud
./start.sh
```

จากนั้นเปิด: http://localhost:3000

**Happy Coding! 🚀**
